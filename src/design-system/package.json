{
  "name": "voice-worker-design-system",
  "version": "1.0.0",
  "description": "Design tokens and components for Voice Worker",
  "scripts": {
    "build": "style-dictionary build --config style-dictionary.config.js",
    "build:tokens": "style-dictionary build --config style-dictionary.config.js",
    "watch": "style-dictionary build --config style-dictionary.config.js --watch",
    "clean": "rm -rf dist",
    "test": "node test/design-system.test.js",
    "test:smoke": "node test/smoke-test.js",
    "test:extract": "node extract-tokens.js",
    "validate": "npm run test:smoke && echo '✅ Design system validation complete'"
  },
  "devDependencies": {
    "style-dictionary": "^3.8.0",
    "glob": "^10.3.0"
  },
  "files": [
    "dist/**/*",
    "tokens.json"
  ],
  "main": "dist/tokens.js",
  "types": "dist/tokens.d.ts"
}
